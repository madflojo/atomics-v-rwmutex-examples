/*
Package full implements the intentionally buggy atomic balance that also
tracks transaction counts and timestamps so it can be compared against
other “full” implementations feature-for-feature.
*/
package full
